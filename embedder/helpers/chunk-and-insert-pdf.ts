import type { TJSONData } from "../types/base";

export const chunkAndInsertPdf = async ({
	fileName,
	data,
}: {
	fileName: string;
	data: TJSONData;
}) => {
	// to properly chunk and insert properly.
};
